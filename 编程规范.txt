在使用共享指针的项目中，不应该出现delete和new
所有对象都是变量或者智能指针变量，再加上没有出现delete，自然内存就管理好了

对于仅使用指针，但不管理该指针生存周期的对象，则应该使用普通指针。
对于使用指针，并且负责该指针生存周期的对象，应该使用智能指针。
对于类内创建，类内使用的成员，应该使用成员变量
对于类外创建，类内使用的成员，应该使用普通指针
对于类内创建，类外提供的成员，应该使用共享指针